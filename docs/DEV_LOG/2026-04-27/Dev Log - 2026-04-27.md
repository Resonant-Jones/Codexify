Dev Log - 2026-04-27

Summary

Schema cleanup and architecture truth-alignment day.

Today was mostly about retiring a dead storage artifact cleanly. The old tool_jobs table moved from historical residue into a formal upgrade/downgrade story, and the surrounding architecture docs were updated so they now describe the resolved state instead of leaving the table hanging as implied debt.

The other thread was research-only work. I captured a large Claude Code source analysis as a future-shaping reference for Codexify. That does not change the runtime, but it gives the longer-range platform discussion a concrete architectural map instead of vague inspiration.

What I completed

Legacy tool_jobs cleanup

- Added d7e8f9a0b1c2_drop_tool_jobs_table.py to drop tool_jobs on upgrade when it exists, remove its legacy index and constraint surface first, and recreate the historical shape on downgrade.
- Added test_tool_jobs_cleanup_migration.py to prove the full migration round trip against a temporary Postgres database.
- Updated data-and-storage.md, legacy-tools-shim-inventory.md, and tech-debt-and-risks.md so the docs now treat tool_jobs as resolved cleanup history, not active operational debt.

Architecture research capture

- Added CLAUDE_CODE_RESEARCH.md, a source-analysis note that maps Claude Code patterns to Codexify’s longer-term continuity and embodied-experience shape.
- Kept it clearly in the research lane. It is useful for direction-setting, but it is not runtime proof.

Important outcome

tool_jobs is no longer just a dead table sitting around in the background. It now has a formal cleanup path, and the docs agree with the database story.

That matters because the repo is speaking more truthfully. The system stopped carrying a legacy schema artifact as if it were still part of the active design, and it started treating it as retired history with a bounded rollback story.

The research capture matters in a different way. It does not claim anything about the current runtime, but it gives future architectural work a concrete reference point instead of a vague aspiration.

Commits

- 9b1f014ae - fix(storage): drop legacy tool_jobs table
- 290e4521c - Drop legacy tool_jobs table

Notes

- Evidence here is code/test/docs-level only. I did not establish live runtime proof.
- The migration test is integration-oriented and env-gated, so it only runs where Postgres and psycopg are available.
- The research file is substantial, but it should be treated as planning material rather than shipped behavior.
- I did not see unrelated repo failures while reconstructing the day.

Next likely moves

1. Run the migration integration test in a Postgres-backed environment and confirm the round trip passes cleanly.
2. Sweep for any remaining mentions of tool_jobs that still read like live operational state instead of historical context.
3. If the Claude Code analysis is going to feed real roadmap work, distill it into a shorter Codexify-specific architecture note or ADR.
4. Revisit the current-state docs only if there are still claims that conflict with the resolved storage cleanup.

Closing thought

Today was mostly about deleting ambiguity without pretending that means more than it does. One part retired a stale storage surface; the other part recorded a future-facing architectural reference. The result is a cleaner boundary between what the system is now, what it used to be, and what it might become.

Narrative Log

Dev Log - 2026-04-27

This was a day about taking stale structure seriously. The visible work was a storage cleanup, but the real change was in the system's truth boundary. tool_jobs stopped being a ghost that lived only in half-remembered docs and became a properly retired artifact with a formal upgrade path, a downgrade story, and a test that can prove both sides of the transition.

That kind of cleanup matters because dead schema can still shape behavior long after the code has moved on. By updating the storage docs and the risk notes at the same time as the migration, the repo stopped telling two different stories about the same table. The change is still code/test/docs-level rather than live runtime proof, but it is the kind of bounded proof that makes the next operator or engineer less likely to inherit confusion.

The other thread was broader and more speculative. I added a large Claude Code source analysis as research material, not as a claim about current capability. Its value is in direction, not shipment: it gives the longer-term Codexify discussion a concrete reference point for how a continuity platform could grow into embodied and home-server-adjacent layers without dissolving into hand-waving.

Taken together, the day narrowed the gap between the repo and the story it tells. One part removed a dead storage surface from the active schema path. The other part captured a possible future without pretending the future is already built. That leaves the system a little less fictional, and a little easier to extend on purpose.