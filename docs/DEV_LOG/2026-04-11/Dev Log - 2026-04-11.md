**Dev Log - 2026-04-11**

Summary

retrieval policy and identity hardening day

Today was about making retrieval scope and identity explicit instead of implicit. The backend now treats slash-driven overrides as a constrained policy merge, carries the effective policy through trace data, and exposes a canonical retrieval-posture snapshot for operators. That turned a fuzzy set of heuristics into something the system can explain.

The other half of the day was identity propagation. Preferred name, profession, and guardian name now move from the persona shell into the completion request and prompt builder, while the new-thread empty state uses the same signal. The result is less drift between UI copy, request payloads, and prompt assembly.

What I completed

Retrieval policy hardening

- Reworked retrieval override handling into a constrained policy merge instead of a free-form branch.
- Kept thread-first searching active while making widening explicitly controllable.
- Added trace-level effective_policy so the broker and worker can carry the real retrieval state forward.
- Added a canonical retrieval-posture route with an empty state and legacy-field fallback.
- Covered the boundary cases with tests for conversation, project, personal knowledge, empty-state, and thread isolation behavior.

Identity wiring and prompt assembly

- Threaded preferred_name, profession, and guardian_name from the chat shell into the completion request.
- Extended the task and prompt-assembly path so identity context reaches system prompt construction directly.
- Suppressed placeholder identity values instead of treating them as real user data.
- Updated the new-thread empty state so it greets by preferred name when one is available, and falls back cleanly otherwise.

Command Center retrieval posture

- Added a dedicated retrieval-posture hook and typed response surface in the Command Center.
- Rendered posture badges for source mode, boundary label, retrieval override, widening reason, and conversation-only state.
- Added a plain-language explainer so the operator can read the posture without reverse-engineering the trace.
- Expanded tests to cover ok, empty, unsupported, and explanatory states.

Architecture notes and proof trail

- Restored the prior supported-path proof artifact on main so the live evidence stays attached to the branch history.
- Added a control-plane / retrieval-policy architecture note that separates intent, interpretation, policy, execution, and observability.
- Added a future MLX execution-kernel spec to keep the long-term runtime idea clearly outside the current beta promise.

Important outcome

The system stopped treating retrieval scope as an implicit side effect. It now has a policy object, a traceable merge rule, and a readable posture surface in the operator UI. That is a real shift in how the system describes itself.

Identity also stopped living only in the shell. The preferred name, profession, and assistant name now cross the request and prompt boundary explicitly, and the fallback behavior is deliberate rather than accidental. That keeps the first screen, the completion payload, and the model instructions aligned.

Commits

162603ab4 - Apply retrieval override as constrained policy merge  
3d9fca7e7 - Add retrieval posture diagnostics route  
bd9f3355b - Restore 2026-04-10 supported path proof artifact on main  
6f7ced8a4 - Wire user identity settings into Guardian prompt assembly  
d80d7b0fb - Personalize new thread empty state with preferred name  
fa776bb44 - Show retrieval posture in trace workbench  
e5c88edb5 - Personalize new thread empty state with preferred name  
688de85c3 - Add retrieval posture explainer to trace workbench

Notes

- retrieval_posture is still synthesized from trace plus payload_summary; the backend does not emit a native canonical field yet.
- The supported-path proof artifact was restored, but docs/architecture/00-current-state.md still says fresh live evidence on the exact current main tip is stale.
- I did not run a new live Compose proof in this pass.
- The worktree is clean and no unrelated failures surfaced.

Next likely moves

- Emit retrieval_posture directly from the completion-service payload so the route no longer synthesizes it.
- Re-run the supported local Compose beta proof on the current main tip.
- Decide whether effective_policy should be shown alongside posture in the Command Center by default.
- Extend the identity thread to any other prompt-bearing surfaces that still rely on defaults.

Closing thought

Today made the retrieval layer legible instead of merely functional. The system can now say what scope it used, why it widened, and which identity context it honored. That is the kind of change that reduces drift later.

**Narrative Log**

Dev Log - 2026-04-11

Today was a contract day more than a feature day. The work moved retrieval from a collection of hidden decisions into a policy the system can name and explain. That sounds small on paper, but it matters because retrieval is where behavior diverges first when a system starts drifting.

The backend change came first. Retrieval overrides now merge into an effective policy instead of replacing anything outright, and the broker carries that policy through the trace. The new diagnostics route can surface a canonical retrieval-posture snapshot, but it is honest about its source: if the backend has not emitted the canonical field yet, the route synthesizes the posture from the existing trace fields. That is the right shape for now. It keeps the boundary explicit without pretending the backend is further along than it is.

Then the UI caught up. The Command Center now has a narrow hook and a dedicated panel for retrieval posture, and it can explain whether a run stayed in conversation, widened within project scope, or crossed into personal knowledge. The explainer text is derived from the same canonical fields, so the operator view is reading the policy rather than guessing at it. On the chat side, the new-thread empty state stopped being generic when a preferred name exists, and the completion payload started carrying identity fields on purpose instead of inheriting them by accident.

The deeper shift is that identity and retrieval are now flowing through the system as explicit inputs rather than ambient context. Preferred name, profession, and guardian name cross the shell, request, task, and prompt boundary. The architecture note made the plane separation legible too: intent, interpretation, policy, execution, observability. That is useful because it gives future changes a place to belong instead of letting them smear across layers.

What remains open is evidence quality. The supported-path proof artifact is back on main, but the current tip still needs fresh live Compose proof, and the new retrieval-posture path is still code/test-level truth rather than runtime proof. Next step is straightforward: emit the canonical posture directly from the backend and then rerun the live proof so the new contract is backed by current evidence.