Dev Log - 2026-04-30

Summary

release-evidence and contract-hardening day

The day was centered on making the beta surface more truthful under real packaging constraints. The main work was not feature expansion so much as boundary repair: restoring packaged media uploads, hardening image-upload validation, and then documenting what the local beta bundle actually does in the registry and on disk.

The second thread was release-state clarity. I refreshed the current-state note, recorded the private beta evidence, and clarified the webUI distribution story so the beta promise matches observed GHCR behavior instead of assumed pullability. The flow-builder state model work from earlier in the day also pushed the UI toward a more canonical internal representation, which matters because it reduces drift between the editor surface and the underlying draft state.

What I completed

Packaged media upload recovery

- Restored packaged media uploads in `guardian/extensions/contracts.py`, `guardian/routes/media.py`, and `tests/routes/test_media_routes.py`.
- This re-established the contract surface behind document/image upload instead of leaving uploads as a brittle packaged-only gap.
- The resulting change is structural: uploads now have enough contract coverage to behave like a real runtime capability, not a loose UI assumption.

Image upload thread validation

- Hardened image upload validation so missing or invalid thread context is rejected cleanly rather than bleeding through as ambiguous behavior.
- Updated the beta evidence note and route tests to reflect the stricter behavior.
- This made the failure mode more explicit and kept the upload path honest about its dependency on thread scope.

WebUI runtime bundle

- Added the standalone webUI runtime bundle in `docker-compose.webui-runtime.yml`, `frontend/Dockerfile.webui`, `scripts/verification/check_webui_runtime_bundle.sh`, and `tests/ops/test_webui_runtime_compose_contract.py`.
- Added the supporting runtime config plumbing and updated the current-state and beta docs so the bundle is described as an actual supported distribution shape.
- This matters because the browser-only beta path now has a concrete build-and-compose story rather than a hand-wavy runtime promise.

Private beta release evidence

- Recorded the local beta release evidence in `docs/beta/2026-04-30-local-beta-release-evidence.md`.
- Captured the GHCR reality for both image families:
- Runtime image pullability is publicly available from a clean shell.
- WebUI image pullability is not anonymous for this identity and still requires package access.
- The fallback validation path is now explicit, including the local bundle verification script and the limits of what it proves.

Current-state truth alignment

- Updated `docs/architecture/00-current-state.md` and the beta docs to match the release evidence.
- Clarified that the beta distribution is split across the macOS desktop shell and the standalone webUI Docker bundle, with GHCR access requirements called out honestly.
- This reduced the gap between the docs and the actual supported release posture.

Flow builder state model

- Added the canonical flow draft state model in `frontend/src/features/flowBuilder/model/flowDraft.ts` and `frontend/src/features/flowBuilder/hooks/useFlowDraftState.ts`.
- Updated the Flow Builder page, graph canvas, chat dock, parameter rail, and tests to use that model.
- The practical effect is that draft state is now more coherent and less ad hoc, which lowers the chance of editor drift and makes the UI easier to reason about.

Important outcome

The system stopped treating beta distribution as a single assumed path and started naming the actual surfaces, constraints, and access boundaries. That is a real shift: the packaged runtime, the webUI bundle, and the media-upload contract now have explicit edges instead of implied ones.

The release record is also more trustworthy now. The notes distinguish between what was verified from the shell, what was verified through the compose bundle, and what still needs different proof. That kind of separation matters because it keeps future release claims from outrunning the evidence.

Commits

- `3fee74e8f` - fix: harden image upload thread validation
- `d40e1115f` - docs: record local beta release evidence
- `0f6091e6f` - feat: add webui runtime bundle
- `36ba946b5` - feat: add webui runtime bundle
- `0a331733f` - fix: restore packaged media uploads
- `b0bcc94c8` - feat(flow-builder): add canonical flow draft state model
- `2bb3bdb73` - docs: clarify webui ghcr pullability

Notes

- The evidence for the webUI path is partly registry-level and compose-level, not full live browser signoff.
- The beta evidence is intentionally conservative about GHCR access: anonymous runtime pull works, webUI pull does not for this identity.
- The media-upload recovery is backed by route and test changes, but this log should not be read as broader end-to-end release proof beyond the recorded checks.
- The flow-builder state work is code/test-level hardening, not a separate runtime validation pass.

Next likely moves

- Re-run live beta proof for the supported Compose path on the current `main` tip.
- Close the remaining gap between documented beta posture and the live backend container flags.
- Extend live validation for upload, embed, and retrieve on the repaired packaged path.
- Watch for any drift between the canonical flow draft model and the editor surfaces that consume it.

Closing thought

Today was mostly about making the system tell the truth more precisely. The biggest value was not novelty, but tightening the boundary between what Codexify can actually do today and what the docs or packaging story claim it can do.

Narrative Log
Dev Log - 2026-04-30

This was a contract-and-truthfulness day.

The useful work was not flashy. It was the kind of work that makes a system more credible: restoring packaged media uploads, tightening image upload validation, and then writing down the real beta distribution story without smoothing over the parts that are still constrained by registry access. That sequence mattered. The code changed first, then the evidence caught up, then the docs were rewritten to match the evidence instead of the aspiration.

The upload recovery was the clearest structural fix. Packaged media uploads had to become a real contract again, which meant changing the route and extension surface and giving the behavior proper test coverage. Once that landed, image upload validation could be hardened so thread scope is no longer fuzzy. In practice, that means the system is less willing to pretend it understands context it does not have. That is a small but important form of integrity.

The webUI runtime bundle work belonged to the same arc. It turned the browser-only beta path into a concrete compose bundle with its own verification script, Dockerfile, and contract tests. That does not erase the distinction between the desktop shell and the webUI path. It sharpens it. The docs now say what the release actually is: two surfaces sharing one local backend, with GHCR access requirements called out plainly. The package is not being sold as universally pullable when it is not.

The release evidence file was the anchor for that honesty. It records what anonymous pull can do, what this identity cannot do, and what the local fallback validation script does and does not prove. That kind of note is useful because it keeps beta language from sliding into generic confidence. It tells the next person exactly where the evidence stops.

The flow-builder state model work sits slightly beside the release packaging thread, but it belongs to the same discipline. A canonical draft model and its hook make the editor surface less improvisational. That reduces state drift and makes later changes safer because the internal representation is less likely to fragment across components. It is not dramatic, but it is the sort of internal cleanup that pays off when the UI gets more complicated.

By the end of the day, the system was not bigger. It was more legible. The packaging story, the upload path, and the editor state all had firmer boundaries. That is the kind of progress that matters when the goal is a local-first system that can survive contact with reality.

What this makes possible next is straightforward: live proof on the repaired beta path, followed by another pass at the remaining runtime posture mismatch. The surface is now specific enough to verify without guessing what it is supposed to be.

::inbox-item{title="Daily dev log captured for April 30" summary="Ready for review; beta evidence and contract shifts are documented"}