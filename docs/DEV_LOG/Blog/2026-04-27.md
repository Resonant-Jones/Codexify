# Daily Log — April 27, 2026

This was a day about taking stale structure seriously. The visible work was a storage cleanup, but the real change was in the system's truth boundary. tool_jobs stopped being a ghost that lived only in half-remembered docs and became a properly retired artifact with a formal upgrade path, a downgrade story, and a test that can prove both sides of the transition.

That kind of cleanup matters because dead schema can still shape behavior long after the code has moved on. By updating the storage docs and the risk notes at the same time as the migration, the repo stopped telling two different stories about the same table. The change is still code/test/docs-level rather than live runtime proof, but it is the kind of bounded proof that makes the next operator or engineer less likely to inherit confusion.

The other thread was broader and more speculative. A large Claude Code source analysis was added as research material, not as a claim about current capability. Its value is in direction, not shipment: it gives the longer-term Codexify discussion a concrete reference point for how a continuity platform could grow into embodied and home-server-adjacent layers without dissolving into hand-waving.

Taken together, the day narrowed the gap between the repo and the story it tells. One part removed a dead storage surface from the active schema path. The other part captured a possible future without pretending the future is already built. That leaves the system a little less fictional, and a little easier to extend on purpose.

## Commits

- `9b1f014ae` — fix(storage): drop legacy tool_jobs table
- `290e4521c` — Drop legacy tool_jobs table
