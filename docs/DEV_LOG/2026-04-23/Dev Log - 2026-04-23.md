**Dev Log - 2026-04-23**

**Summary**

contract hardening day

Today was mostly about removing a schema boundary that had been too loose and then making the surrounding UI and docs tell the truth about it. The central fix was the extension/eval migration merge: the repo now has a clean Alembic merge head, and the migration tests prove the expected `agent_extension_*` and `eval_*` tables exist after upgrading to `heads`.

The other meaningful change was on the Settings surface. The Data tab now explicitly frames System Docs as a high-authority constitutional overlay for the assistant prompt, not a general project corpus lane. That is a small copy change on paper, but it closes a real product boundary and keeps the UI aligned with how the system is supposed to behave.

**What I completed**

**Extension/eval schema consistency**
- Added the Alembic merge migration `b3c4d5e6f7a8` to join the extension and eval heads without changing schema.
- Added integration coverage that upgrades a fresh database to `heads` and asserts the expected extension and eval tables are present.
- Hardened the migration round-trip tests so proposal, decision, registry, and binding rows are committed and refreshed before dependent foreign keys are wired up.

**Settings surface boundary**
- Added a dedicated System Docs card to the Settings Data tab.
- Framed System Docs as constitutional overlays for the assistant prompt, with explicit copy that they are not the normal place for project documents or project knowledge ingestion.
- Added tests that lock that boundary copy in and updated the scroll-body assertions to target the real scroll container.

**Architecture truth refresh**
- Updated the current-state doc to record that the supported Compose migrator now reaches the merged extension/eval head cleanly.
- Refreshed the codemap snapshot so the architecture map stays current with the repo shape.

**Important outcome**

The system stopped treating the extension schema split as an implied relationship between branches and started treating it as one merged head with an explicit check. That matters because the missing-table failure mode is no longer just remembered in prose; it is covered by the migration path itself.

The Settings tab also became more truthful. System Docs now names its own role instead of borrowing the language of project ingestion, which keeps the UI aligned with the boundary the product already wanted.

**Commits**

- `ffcf463c7` - clarify system docs as constitutional overlays
- `1721a9c67` - Merge pull request #300 from Resonant-Jones/codex/fix-agent-extension-schema-gap
- `983beb047` - Refresh codemap snapshot

**Notes**

- The migration fix is backed by integration tests, but no fresh live Compose validation was run in this pass.
- The release-state doc still flags the supported-profile recheck and bounded tool-loop regressions as open.
- `guardian/codemap/codemap.json` is a generated snapshot refresh, so that commit is mechanical rather than semantic.

**Next likely moves**

- Rerun the supported Compose upload/retrieve proof on the current tip.
- Recheck the live backend posture flags against the supported local-only profile.
- Continue on the bounded tool-loop regression.
- Verify the Settings copy in the in-app browser if UI proof is needed beyond tests.

**Closing thought**

This was mostly a day of removing ambiguity. One real blocker was closed, one product boundary became clearer, and the repo’s truth layers were brought back into alignment.

**Narrative Log - 2026-04-23**

Today was a truth-alignment day more than a feature day. The strongest work was structural: extension and eval migrations were tied together with an explicit Alembic merge head, and the migration suite was hardened so it now proves a clean upgrade path to `heads` and the presence of the expected tables. That is a small piece of plumbing, but it matters because the missing-table failure mode had been blocking document upload from reaching retrieval at all.

The release-state doc was adjusted to match that reality. It no longer describes the schema gap as the current blocker on the supported Compose path; instead it records that the merged head now lands cleanly while the supported-profile recheck and tool-loop regressions remain open. That distinction is important. The fix removes one specific failure class, but it does not pretend the runtime is fully signed off.

The other visible change was on the Settings surface. The Data tab now carries a dedicated System Docs card with copy that makes the boundary explicit: these are constitutional overlays for the assistant prompt, not the ordinary place for project documents or project knowledge ingestion. That language is not cosmetic. It keeps a high-authority prompt surface from being mistaken for a general corpus lane, and the tests now lock that boundary in.

I also refreshed the codemap snapshot so the repo map reflects the current shape of the tree instead of a stale snapshot. Taken together, the day was about restoring alignment between code, tests, and docs. The next step is not more interpretation. It is rerunning the supported Compose proof against the current tip and closing the remaining runtime mismatches one by one.

::inbox-item{title="Daily dev log captured" summary="Logs drafted from today’s commits; schema gap and UI boundary noted"}