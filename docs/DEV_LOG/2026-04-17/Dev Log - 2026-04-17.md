**Dev Log - 2026-04-17**

**Summary**  
Contract hardening day.

This was a day of removing hidden failure modes from the shell rather than adding new surface area. Flow Builder got a reliable way back to Guardian, the composer stopped reordering itself under width pressure, and Persona Studio’s mounted structure was made explicit enough that regressions should show up in tests instead of as silent drift.

**What I completed**

**Flow Builder exit path hardening**

- bd91c12b2 guarded Flow Builder routing when leaving the page.
- 23784cf7d restored a visible Back to Guardian escape hatch and kept the prior Guardian /chat/<id> route reachable after focus moves into the draft fields.
- The route now canonicalizes its mode query and stays aligned with browser history instead of pulling the user back into the builder.
- Why it matters: Flow Builder is now a bounded spec seam with a real exit.

**Composer rail and send slot stabilization**

- f3551a1a4 realigned the send button.
- 9fc26a0e1 hardened the composer rail against narrow-width reordering.
- The control strip now keeps the source/provider controls ahead of an anchored send slot, and the tests cover both desktop and mobile compression behavior.
- The source selector also stays stable across sends in the same thread-scoped harness.
- Why it matters: the composer can shrink without changing the interaction order, which is the difference between a tight layout and a confusing one.

**Persona Studio render safety**

- d7f66cfa8 tightened Persona Studio render safety and the surrounding composer layout.
- The page now mounts as a clearer two-lane shell with an editor lane, a session-local ephemeral chat harness, and a subordinate utility pane for profiles and diagnostics.
- A new render-safety test asserts that the edited shell, truth matrix, utility pane, and ephemeral harness all mount together.
- Why it matters: Persona Studio now has enough structural truth to fail loudly if a future refactor drops part of the surface.

**Important outcome**  
Flow Builder is no longer just a page you can enter. It now preserves an explicit way out even after the draft area takes focus, which keeps the shell from becoming a dead end.

The composer stopped acting like a loose cluster of controls and became a stable control surface. That matters because width compression no longer changes the user’s mental map of where the send action lives.

Persona Studio now has a stronger mount-time contract. The system still needs live proof on the current tip, but the shell itself is better defended against the kind of layout drift that hides missing state until someone stumbles into it.

**Commits**

- bd91c12b2 - Guard Flow Builder routing when leaving the page
- 23784cf7d - Restore Guardian escape from Flow Builder
- f3551a1a4 - Realign composer send button
- 9fc26a0e1 - Harden composer rail against narrow-width reordering
- d7f66cfa8 - Fix Persona Studio render safety and composer layout

**Notes**

- Evidence today is code/test-level only; there was no fresh live runtime proof on the current main tip.
- I did not find a new 2026-04-17 daily audit artifact in the repo, so there is no extra operator log to cite.
- The current-state note at 00-current-state.md still says fresh live release evidence is required before signoff.

**Next likely moves**

- Run a real shell smoke pass through Flow Builder and back to Guardian.
- Verify the composer rail at narrow desktop and mobile widths in-browser.
- Exercise Persona Studio in the installed app or local runtime to confirm the render-safety contract matches live mounting behavior.
- Update the current-state note if this work is meant to carry release truth forward.

**Closing thought**  
Today was about making the shell more truthful. The system is easier to trust when route exits, control ordering, and mounted panels all survive compression and refactor pressure without relying on luck.

**Narrative Log - 2026-04-17**

This was a day of tightening the shell around places where it had been too permissive. The first move was Flow Builder: it stopped assuming the surrounding shell would stay in view and got its own way back to Guardian. That sounds small until you look at the failure mode it removes. A route that can strand the user is not just awkward; it is a broken boundary. The new behavior keeps the prior Guardian path reachable even after the draft fields take focus, which turns Flow Builder from a detour into a bounded lane.

After that, the work moved into the composer, where width pressure had been making the control surface less legible than it should be. The send button was realigned, then the whole rail was hardened so the controls do not reshuffle themselves when the viewport gets tight. The important part is not the visual polish. It is that the interaction order now stays stable as the space compresses. That keeps the user’s mental map intact, which is what a serious composer surface should do.

Persona Studio got the final pass, and that one was about render truth. The shell now presents itself as a clearer two-lane structure with a session-local ephemeral harness and a subordinate utility pane for profiles and diagnostics. A new render-safety test makes the mount contract explicit: the edited shell, truth matrix, utility pane, and ephemeral harness all have to appear together. That does not equal live proof, and it should not be mistaken for it. What it does is reduce the odds of a future refactor quietly dropping part of the surface and only revealing the damage in manual use.

The deeper shift across the day is that three different parts of Codexify stopped relying on incidental layout behavior. Flow Builder now has a credible exit. The composer now keeps its control order when the width collapses. Persona Studio now has a stronger structural contract around what it mounts. That is the kind of work that does not look dramatic in isolation, but it lowers drift and makes the system more trustworthy in the places users feel immediately.