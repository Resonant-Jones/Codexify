  
Dev Log - 2026-04-03

Summary

runtime truth-alignment day

Most of today was about making the system describe itself more honestly. The import path, health surface, sidebar provenance, and supported RAG proof all moved in the same direction: fewer loose interpretations, more explicit contracts, and one live proof that the supported retrieval surface actually works.

This was less about adding surface area than about reducing drift. The day mattered because it tightened the gap between backend reality, operator visibility, and the evidence trail that has to support any serious release claim.

What I completed

### Import pipeline hardening

- backend/rag/chatgpt_migration.py now processes ChatGPT embedding work in batches and records explicit candidate, persisted, and failed counts.
- The migration route and startup sweep tests were tightened around the supported single-user profile, so import recovery stays inside the release boundary instead of drifting into implicit behavior.
- The practical effect is narrower failure mode: large imports and partial embedding failures are now visible and recoverable instead of quietly blending into partial success.

### Bootstrap and migration hygiene

- Fix Alembic heads and chat indentation kept the backend startup and migration path coherent while chat code was being touched.
- This is support work, but it matters because release-gate work depends on the backend starting cleanly and migrations lining up without ambiguity.

### Runtime visibility and health

- guardian/core/health_service.py and guardian/routes/health.py now normalize health payloads into one status vocabulary instead of leaking raw vendor strings upward.
- The Command Center health hook and panel now interpret /health, /health/llm, /api/health/llm, /health/vector, and /health/memory through that shared contract and expose raw payloads for inspection.
- The result is a more truthful operator view: status labels now match backend semantics instead of being inferred ad hoc in the UI.

### Imported thread provenance

- frontend/src/components/sidebar/sidebarPresentation.ts now turns imported thread metadata into canonical provenance labels.
- frontend/src/components/sidebar/useSidebarThreads.ts, frontend/src/components/sidebar/ThreadList.tsx, and frontend/src/components/sidebar/SidebarRoot.tsx use that labeling to filter imported threads by source/provider.
- That turns the imported-thread list from a flat bucket into something you can actually audit and scope.

### Audit trail and release bookkeeping

- The daily audit snapshot for 2026-04-03 was refreshed, the competitive parity audit was added, and the audit JSON newline issue was cleaned up.
- These are documentation-level changes, but they keep the evidence trail machine-readable and aligned with current work.

### Supported RAG proof

- docs/architecture/2026-04-03-supported-rag-path-proof.md captures a live compose proof on the supported retrieval surface.
- It shows supported-profile flags active, quarantine still holding, thread creation and assistant persistence working, document upload and embedding reaching ready, and retrieval succeeding on /api/health/retrieval?q=....
- The backend and worker vector-store runtime matched, which is the part that makes the proof meaningful.

Important outcome

The system is no longer relying on a loose story about what should be healthy, importable, or retrievable. It now has a narrower runtime contract, a more honest health surface, provenance-aware sidebar filtering, and a live retrieval proof on the supported path.

That does not make it release-ready. It does mean the supported path has enough structural truth to be argued from evidence instead of assumption, which is the real shift today.

Commits

- b01818ef5 - fix(import): batch ChatGPT embedding normalization
- 35f32d0e8 - Update daily audit snapshot for 2026-04-03
- afe052afb - Fix Alembic heads and chat indentation
- eb8a96e2d - docs(audit): add competitive parity audit
- 6c30fc202 - normalize health endpoints and status interpretation
- 2548d6a2d - Fix audit JSON newline
- f0817a331 - Add provider filters for imported sidebar threads
- 977777c84 - docs(architecture): add supported rag path proof on current head

Notes

- The live RAG proof was captured in Docker Compose; host-port curl was not reachable from the shell, so the final probes ran inside the backend container.
- docs/architecture/00-current-state.md still reflects pre-proof blocker language and needs a refresh.
- The morning audit snapshot predates the final docs-only proof commit, so it is useful context but not the last word on today's state.
- I did not run a fresh test suite in this session; the log is synthesized from today’s commit stream and proof/audit artifacts.

Next likely moves

1. Refresh docs/architecture/00-current-state.md to fold in the supported retrieval proof.
2. Reconcile the release-signoff surface between /api/health/retrieval and any remaining /api/retrieve expectation.
3. Keep health alias drift under control in both backend routes and Command Center expectations.
4. Validate that provenance labels line up with the import metadata you actually care about.
5. Exercise the ChatGPT import batching and retry path under a larger export, not just fixture-sized inputs.

Closing thought

Today’s work made the system easier to trust because it reduced the gap between backend reality, operator visibility, and the evidence trail. That is the kind of progress that survives the next failure instead of hiding it.

Narrative Log

Dev Log - 2026-04-03

Today was a day of tightening contracts rather than widening scope. The earliest work went into the import path, where backend/rag/chatgpt_migration.py was pushed toward batch-aware embedding normalization and explicit accounting for what persisted and what failed. That matters because imports stop being a binary success story and start being a system that can describe partial failure without hiding it.

The next layer was runtime hygiene. Fix Alembic heads and chat indentation is the sort of change that disappears when it works, but it keeps the startup and migration path from quietly fracturing while higher-level work is happening. In the same span, health handling was normalized through guardian/core/health_service.py and guardian/routes/health.py, and the Command Center was taught to read that same vocabulary instead of inventing its own. That is a small move on the surface and a large one underneath, because it makes the operator view line up with backend truth instead of approximating it.

The sidebar work followed the same logic. frontend/src/components/sidebar/sidebarPresentation.ts now gives imported threads canonical provenance labels, and frontend/src/components/sidebar/useSidebarThreads.ts uses those labels to filter the list by source or provider. That turns imported conversations from a flat archive into something closer to an audit surface. It is not flashy, but it removes ambiguity where ambiguity tends to become operational debt.

The day closed with the most important correction: a live supported RAG proof on the current runtime state, captured in docs/architecture/2026-04-03-supported-rag-path-proof.md. The proof showed the supported profile active, quarantine still doing its job, thread creation and assistant persistence working, document upload reaching embedding readiness, and retrieval returning signal through the supported health surface. The backend and worker agreed on the same vector-store runtime, which is the piece that makes the whole proof count. That is the moment the day stopped being about cleanup and became about evidence.

The audit and parity docs, plus the audit JSON cleanup, sat around that core work as bookkeeping. Useful bookkeeping, because the longer-term state file still needs to catch up. What this makes possible next is not a broader claim, but a cleaner one: the supported path can now be described from proof instead of hope, and the remaining work is mostly about folding that proof back into the canonical short-horizon state.