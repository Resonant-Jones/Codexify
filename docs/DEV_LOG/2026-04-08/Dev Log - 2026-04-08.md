Dev Log - 2026-04-08

Summary

Quarantined-surface hardening day.

This was mostly about taking the personal facts settings surface from a static lifecycle mock to a live, controllable backend-facing panel without changing the supported beta contract. The work tightened the boundary between quarantine and runtime trust, and made the system more truthful about what is approved, what is verified, and what is still just evidence.

What I completed

Personal facts lifecycle surface
- Added `Personal Facts` to the settings dock and made the dock sticky and keyboard-roving so it behaves like a first-class settings surface.
- Reworked the panel into explicit `Candidates`, `Verified`, and `History` sections with summary stats, evidence disclosure, and loading/error states.
- Kept the lifecycle language sharp: candidates stay quarantined, verified facts are runtime-eligible, and history remains an audit trail.

Live data wiring
- Added `usePersonalFacts` and new API helpers for list/detail/evidence/revision fetches plus confirm, dispute, archive, and amend mutations.
- The panel now loads live `/api/personal-facts` data, refreshes after mutations, and tracks busy state per fact instead of depending on fixed placeholders.
- The implementation now reflects actual backend records rather than a static narrative of the lifecycle.

Test coverage and guardrails
- Expanded panel tests to cover live-data rendering, section switching, evidence toggles, edit-and-approve flows, and empty/loading states.
- Tightened dock tests to assert the sticky layout and the new Personal Facts tab behavior.

Important outcome

Personal facts is no longer just a mock settings panel. It now speaks to live records and exercises the approve, dispute, amend, and retire paths against the backend.

The deeper change is that the system now has an explicit boundary between quarantined evidence and runtime-trusted facts. That boundary is visible in the UI and enforced in the data flow, which makes drift easier to notice and harder to hide.

This does not mean the surface has been promoted into the supported beta. It means the implementation is now honest enough to be evaluated on real behavior instead of placeholders.

Commits

6d7a2f53b - Add personal facts lifecycle panel to settings
4f98ad908 - Wire personal facts settings panel to live data

Notes

- [`/Users/resonant_jones/.codex/worktrees/9f55/Codexify/docs/architecture/00-current-state.md`](/Users/resonant_jones/.codex/worktrees/9f55/Codexify/docs/architecture/00-current-state.md) still says `personal_facts` is quarantined from the supported beta surface.
- The evidence here is code/test-level; I did not rerun live end-to-end runtime proof on the current `main` tip.
- No unrelated failures surfaced in the local worktree.

Next likely moves

- Re-run the supported local Compose proof on the current `main` tip.
- Decide whether `personal_facts` stays quarantined or gets promoted into the supported release promise.
- Validate the backend `personal-facts` routes against live data and failure cases.
- If the surface remains operator-only, document that boundary so it is not mistaken for beta support.

Closing thought

The day moved personal facts from presentation into enforcement. The UI now encodes the lifecycle, and the backend hooks give that lifecycle real state to act on. The remaining question is whether this stays outside the supported contract or becomes part of it.

Narrative Log - 2026-04-08

Today was about making a boundary honest.

The personal facts area in Settings stopped being a static explanation and became a real lifecycle surface. The dock gained a Personal Facts tab, the panel was reorganized around candidates, verified facts, and history, and the copy was tightened so the product says what it means. Candidates are quarantined. Verified facts are runtime-eligible. History is a ledger. That sounds subtle, but it matters: it turns the page from decorative UI into an explicit trust model.

The second commit made that trust model operational. A new `usePersonalFacts` hook and matching API helpers now fetch live `/api/personal-facts` data, pull detail, evidence, and revision records, and drive the approve, dispute, amend, and retire actions. The panel refreshes after mutations, tracks per-fact busy state, and handles loading and error cases instead of leaning on fixed sample data. In other words, the surface now talks to the backend as a state machine rather than as a storyboard.

The tests followed the same shift. They were expanded to cover live-data rendering, section switching, evidence disclosure, edit flows, and empty states. The dock tests were also updated to reflect the sticky layout and the new tab. That is not just wider coverage. It is the codebase admitting that this surface has behavior worth protecting.

What changed underneath the surface is the system’s honesty about identity data. Personal facts now has a visible lifecycle and a visible boundary between quarantined evidence and runtime-trusted facts. That makes drift harder to hide and makes future decisions about promotion or exclusion much cleaner.

The release contract has not changed with it. The current-state doc still treats `personal_facts` as quarantined from the supported beta surface, so this should be read as implementation hardening, not as a claim of release inclusion. The next step is to prove the current tip against the supported runtime path and decide whether this surface stays operator-only or gets folded into the beta promise.

::inbox-item{title="Personal facts lifecycle hardened" summary="Live wiring landed; decide whether it stays quarantined"}