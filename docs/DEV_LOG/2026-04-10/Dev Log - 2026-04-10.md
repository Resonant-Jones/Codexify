Dev Log - 2026-04-10

Summary

contract hardening day

Today was about making retrieval routing, thread rails, and shell state line up with the contracts the app already wants to enforce. The backend got a cleaner story for source mode vs retrieval override, and the frontend shed duplicate controls and regressions around the active-view shell. The day also brought the current-state docs into sync with the new backend-seam evidence, without widening the release promise.

What I completed

**Retrieval posture and source-mode truth**
- Applied retrieval overrides in completion orchestration and queued chat tasks so the effective mode is resolved in one place instead of being inferred from scattered state.
- Canonicalized retrieval posture tokens and added conversation-only behavior so the broker/source-mode matrix stays consistent across the trace path.
- Extended trace metadata and tests to carry task-routing context, including the effective source mode needed to explain why a completion used project context or personal knowledge.
- The first snapshot attempt was reverted and reworked before the final merge; the end state is cleaner, but the evidence is still backend-seam code/test-level rather than live runtime proof.

**Active-view and thread-rail cleanup**
- Enforced the active-view contract and dismissible thread rails, then backed out the regression when that path proved too noisy in sidebar and documents.
- Removed redundant workspace and speaker rail actions from the chat surface, which narrows the control surface and reduces duplicate affordances.
- Documents, sidebar, and chat now imply fewer states they do not own.

**Shell, mobile, and settings legibility**
- Simplified Dashboard, Documents, and Settings shell layouts, including the mobile-specific syntax and layout cleanup that landed through the merged frontend work.
- Restored settings tab persistence and regression coverage so the shell state holds across re-entry instead of resetting under navigation churn.
- Polished mobile motion, gesture behavior, bottom-edge handling, and viewport inset behavior so the phone shell is less brittle.

**Evidence alignment**
- Aligned the current-state and risk docs with the new backend-seam evidence.
- The docs still correctly keep `personal_facts` outside the supported beta promise, and they still say fresh live runtime proof on the current `main` tip is stale.

Important outcome

The system is less ambiguous than it was this morning. Retrieval routing now has a clearer source of truth, and the trace path can explain why a completion used project context or personal knowledge. That matters because it turns a fuzzy behavioral question into something the backend can account for.

The frontend moved in the same direction. The active-view experiment was narrowed back down, redundant rail actions were removed, and the shell stopped advertising more control than it could consistently support. The result is not flashy, but it is structurally cleaner: fewer duplicates, fewer hidden toggles, less drift between the UI and the actual state machine.

Commits

- 39ecc0e16 - Remove redundant workspace and speaker rail actions
- e58ac75e3 - Revert thread rail regression in sidebar and documents
- 57d6fd2d1 - Merge pull request #258 from Resonant-Jones/codex/enforce-activeview-contract
- b4d80901a - Merge pull request #262 from Resonant-Jones/codex/add-retrieval-posture-contract
- 046f55c27 - Apply retrieval override in completion orchestration
- 92001e79d - Persist retrieval override on queued chat tasks
- 2e9b23bb5 - Canonicalize retrieval posture tokens
- e6ed492b2 - Add conversation-only retrieval posture
- c89f78593 - Add retrieval posture contract snapshot
- c1c283bb3 - Revert "Add retrieval posture contract snapshot"
- 475d0efef - Merge pull request #260 from Resonant-Jones/codex/refactor-settings-shell
- c017d043a - Merge pull request #259 from Resonant-Jones/codex/fix-dashboardview-syntax
- 8c57b1f25 - Merge pull request #261 from Resonant-Jones/codex/fix-mobileshell-syntax
- 4e6e958d0 - Persist active settings tab across re-entry
- d3b7e5694 - Add settings tab persistence regression coverage
- bb018becd - Align current state and risk register with evaluation evidence
- 1398566e4 - Fix backend CI regressions in trace persistence and command bus gating

Notes

- Some of the retrieval-contract work went through a revert/rework loop before landing cleanly.
- Today's evidence is still code/test-level and doc-level; no fresh live runtime proof on the exact current `main` tip.
- `personal_facts` was touched in the settings shell, but it remains quarantined from the supported beta promise.

Next likely moves

- Rerun the supported local Compose beta proof on the current `main` tip.
- Exercise the retrieval trace path end to end against the current source-mode / retrieval-override contract.
- Keep thread-rail and mobile-shell regression coverage green.
- Decide whether `personal_facts` stays quarantined or gets an explicit supported-profile update.

Closing thought

Today was about removing duplicated authority. The code now states its contracts more directly, the docs reflect the same boundary, and the remaining work is to prove that truth in the live stack rather than only in tests.

Narrative Log

Dev Log - 2026-04-10

Today was a boundary day more than a feature day. The useful work was not adding surface area; it was stripping away ambiguity so the system could explain itself more cleanly. That showed up first in the retrieval path, where source mode and retrieval overrides stopped living as loosely related hints and started being resolved as one explicit orchestration decision. The trace payloads followed suit, carrying the metadata needed to show why a completion went to project context or personal knowledge.

The retrieval work was not linear. There was a snapshot attempt, a revert, and then a cleaner merge path that held up better under the current contract. That is fine. The important thing is that the contract now reads as something the backend can enforce and the tests can observe, not something that has to be inferred after the fact. The current-state and risk docs were updated to match that reality, while still making clear that the live runtime proof on the exact current `main` tip has not been rerun.

On the frontend, the same pressure toward clarity showed up in the active-view and thread-rail work. The first pass made the contract explicit; the follow-up cleanup removed the parts that had become noisy or misleading. By the end of the day, the sidebar, documents view, and chat header were carrying fewer duplicate controls. Workspace and speaker actions were pared back, the thread-rail regression was backed out, and the shell stopped suggesting states it did not actually own.

The mobile and settings work fit the same pattern. Dashboard, Documents, and Settings were simplified so the shell state stayed legible on smaller layouts, the settings tab held its state across re-entry, and mobile motion and inset behavior were tightened so the UI felt more deterministic. None of that is flashy. It is the kind of work that prevents the next layer of confusion from forming.

The larger shift is straightforward: the system is less theatrical and more truthful. Retrieval is better accounted for. The UI shows fewer false choices. The docs acknowledge what is proven and what is still only partially proven. That leaves the next step obvious: rerun the live beta proof on the current tip and make sure the cleaner contracts survive contact with the running stack.

::inbox-item{title="Daily dev log captured" summary="Today's record is ready; live runtime proof still needs rerun"}