# Tester provider/model authority restoration into current architecture line

## Result

**PASS — source-side architecture reconciliation only.**

This record restores the accepted Tester provider/model authority doctrine
from a divergent local source lineage. It is not a live Whoosh'd inventory,
Guardian health, service, or completion qualification.

## Source reconciliation

- **Current branch at preflight:** `codex/define-github-watchdog-control-plane`
- **Watchdog prerequisite:** `e17d7bc66aeb05b35f579b567522ef3e3af102d7`,
  present in current ancestry.
- **Historical authority source:**
  `ec20badb4b04b1ea2676bf99f6ab7a582ff1cf54` — `Canonize tester model configuration authority`.
- **Historical parent:** `e146d9b5aa58e54d91fa2443db0278e1807a8041`.
- **Lineage relation:** historical source exists locally and is deliberately
  non-ancestor of this branch.

The historical ADR used the identifier ADR-072. This line already assigns
ADR-072 to the accepted Connections route-promotion decision and ADR-073 to
the accepted Watchdog control-plane decision. The first free identifier is
ADR-074, so the same Tester doctrine is now canonical as ADR-074. No existing
ADR was renumbered or reinterpreted.

## Historical-file classification

| Historical path | Classification | Current treatment |
| --- | --- | --- |
| Tester supported profile | PORT | Restore the historical Qwen tracked default and authority commentary. |
| Whoosh'd/DeepSeek Compose overlay | PORT | Derive the four chat aliases from `${LOCAL_CHAT_MODEL}`; keep vision/GGUF independent. |
| Tester runbook | PORT | Describe authority without claiming current model availability. |
| Historical Tester ADR-072 | PORT | Restore substantively as current ADR-074 with provenance. |
| ADR index | PORT / UNRELATED | Add only ADR-074 entries; do not import the historical ADR-071 wording change. |
| Config and Ops | PORT | Record precedence and fail-closed semantics under ADR-074. |
| Historical runtime proof | SUPERSEDED | Preserve as historical source evidence; do not copy its runtime PASS into this branch. |
| Startup profile script | PORT | Restore Qwen as an overrideable assertion default, not Compose authority. |
| Supported-profile provider tests | PORT | Re-establish deterministic authority tests without treating Qwen as current inventory truth. |
| Tester services tests | PORT | Re-establish static Compose-alias and DeepSeek-posture checks. |

No Guardian source, provider registry, router, health route, Watchdog file,
runtime service, `.env.tester`, or Whoosh'd configuration was changed.

## Restored doctrine

ADR-074 preserves one concrete Tester local-chat-model authority:

```text
supported profile = allowed/default provider posture
operator .env.tester = concrete local chat-model selection
Compose = transport only
Guardian health/catalog + live Whoosh'd inventory = fail-closed runtime truth
```

The historical tracked Qwen default is restored only to restore the accepted
configuration mechanism. Current live serving state and any Gemma/Qwen
reconciliation remain unobserved in this task.

## Deferred runtime work

The next independently scoped slice must compare the restored authority
contract against current live Whoosh'd inventory, update the operator selection
only if authorized by that evidence, and prove one ordinary persisted local
completion before Watchdog qualification resumes.
