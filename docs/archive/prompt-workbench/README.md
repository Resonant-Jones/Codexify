# Prompt Workbench Archive

`docs/archive/prompt-workbench/` keeps exploratory prompt notes, mirrors, and historical prompt artifacts that are not the canonical prompt surface anymore.

For stable prompt and governance docs, use `docs/reference/prompts/`.
