# Prompt Docs

`docs/reference/prompts/` holds the canonical prompt and prompt-governance material that should remain linkable and stable.

Use `docs/archive/prompt-workbench/` for exploratory prompt notes, mirrors, and scratch documents that are still worth retaining but should not be treated as source-of-truth.
