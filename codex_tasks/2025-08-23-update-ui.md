
codex

### Context

* I’m in the monorepo root of **guardian-backend_v2**.
* Front-end code lives in `./src` (React + Vite + Tailwind v4).
* The Tauri wrapper lives in `./src-tauri`; do **not** touch Rust or Tauri config in this task.
* I added two screenshot folders:

codex_tasks/
├─ UI-Sample-CurrentState/   # how the app renders now
│    ├─ dashboard-page.png
│    ├─ guardian-page.png
│    └─ settings-page.png
└─ UI-Sample-GoalState/     # what it SHOULD look like
├─ Dashboard.png
├─ Guardian.png
├─ Settings-DarkMode.png
└─ Settings-LightMode.png

### Goal

Bring the UI in `./src` up to the **Goal State** mocks (files above) while preserving all existing business logic and component names.

### High-level requirements

1. **Global styles**

* Tailwind pre-flight must load (it currently doesn’t render).
* Tokens from `:root` in `src/index.css` stay authoritative.

2. **Layout**

* Header: fixed height, span full width; use gradient bar exactly as in mocks.
* Main split:
  * Left = the page itself (Dashboard / Guardian / Settings).
  * Right = Workspace pane (only on Dashboard + Settings), glass effect.
* Chat page (Guardian) must **NOT** have the glass overlay.

3. **UI primitives**

* `src/components/ui/{button,card,input,textarea,badge,separator}` get Tailwind
   defaults that match the Goal State (rounded-xl, neutral 900 text, etc.).

4. **Pages**

* Update markup in:
  * `src/features/dashboard/DashboardView.tsx`
  * `src/features/chat/GuardianChat.tsx`
  * `src/features/settings/SettingsView.tsx`
* Do **not** rename files; adjust JSX + classes only.

5. **Glass**

* `ReactiveGlassCard` keeps WebGL path; add CSS-only fallback matching mocks.

### Deliverables

* Patches to the files above + any new utility CSS classes in `src/index.css`.
* No **src-tauri** changes.
* All changes compile with `pnpm -C ./src typecheck && pnpm -C ./src dev`.
* Tailwind works in the browser (verified by seeing `@tailwind base` in loaded CSS).

### Done when

* Running `pnpm tauri dev` renders pages that visually match the Goal State PNGs.
* All existing Vitest tests pass (`pnpm -C ./src test`).

### Notes for Codex CLI

* You can open the PNGs directly (they’re in repo) if needed for colors/spacing.
* Use `clsx` or template-string classNames; keep inline-style hacks out.
* Keep commits small and atomic; feel free to push multiple commits.

### First step

Patch only the **Tailwind/CSS-loading issue** so the current UI stops rendering un-styled text. Once that works, proceed with the rest of the tasks above.

