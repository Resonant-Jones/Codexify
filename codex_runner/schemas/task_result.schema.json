{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Campaign Runner Task Result",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "status",
    "summary",
    "tests_ran",
    "commit_hash",
    "implementation_commit_hash",
    "receipt_update_commit_hash",
    "notes"
  ],
  "properties": {
    "status": {
      "type": "string",
      "enum": [
        "success",
        "failed",
        "blocked"
      ]
    },
    "summary": {
      "type": "string"
    },
    "tests_ran": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "commit_hash": {
      "type": "string",
      "description": "Implementation commit hash. May be empty; the runner may fill this based on HEAD after auto-commit.",
      "minLength": 0
    },
    "implementation_commit_hash": {
      "type": "string",
      "description": "Explicit implementation commit hash (preferred over commit_hash when present). Runner may fill.",
      "minLength": 0
    },
    "receipt_update_commit_hash": {
      "type": "string",
      "description": "Commit hash for runner-authored receipt updates (task artifact + campaign mapping). Runner typically fills this.",
      "minLength": 0
    },
    "notes": {
      "type": "string"
    }
  }
}
